# System Architecture

See the interactive architecture diagram in the dashboard: run the frontend and click the **About** tab.

```bash
cd frontend && npm run dev    # http://localhost:5174 → About tab
```
