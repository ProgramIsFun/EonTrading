#!/usr/bin/env bash
# Pre-push hook: test before pushing.
# Install: git config core.hooksPath .githooks
set -e

echo "🧪 Running tests..."
PYTHONPATH=. python -m pytest tests/ -q || { echo "❌ Tests failed"; exit 1; }

echo "✅ All checks passed"
